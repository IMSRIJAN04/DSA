class Tree {
    // Function to find the minimum element in the given BST.
    int minValue(Node node) {
        if(node == null) return -1;
        int data = node.data;
        while(node!= null){
            data = node.data;
            node = node.left;
        }
        return data;
    }
}
